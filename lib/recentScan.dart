import 'package:flutter/material.dart';

class RecentScan extends StatelessWidget {
  const RecentScan({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text("Scan Page")));
  }
}
